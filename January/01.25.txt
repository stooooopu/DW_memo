☆★☆★☆★생성자☆★☆★☆★
많이 보는 수밖에 읎다!

1. 생성자는 함수다(특수 함수)

2. 리턴이 없고(void도 없음)

3. 함수이름은 클래스 이름과 동일

4. 기본 생성자(디폴트 생성자)존재

5. 생성자를 사용하는 이유
  => 필드변수에 값을 대입하기 위해서

☆★☆★☆★☆★필드변수는 클래스 안에 있는 모든 메소드에서 사용 가넝☆★☆★☆★☆★☆★
☆★☆★☆★☆★☆★☆★변수옆에 this가 붙어있다 무적건 전역변수☆★☆★☆★☆★☆★☆★
1. 생성자도 함수라 위에 코드가 가능
2. 전역변수여서 생성자에서도 사용 가넝

Pizza p = new Pizza();
1. Pizza클래스를 외부에서 호출하려면
 강제로 생성자를 생성해야 함

2. 강제? -> 필드변수 값을 대입 해줌(목적)
	=> 사용 빈도수는 낮음 하지만 쓰긴 하니까 알긴 알아야제

3. get set 함수 만들어서 대입

클래스 함수 아리쏭~! 그럴 땐 클래스를 이해 할 때까지 똑같은 클래스를 만들고 지우는걸 반복하라노~!!!

☆★☆★메소드 파라미터에 클래스타입이 있다면
new를 이용해서 클래스를 불러온 후
파라미터에 넣어야☆★☆★ 